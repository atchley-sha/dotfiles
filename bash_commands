#Add custom bash commands to be sourced in bashrc

#create BEAM jar and move to convenient directory
alias beamjar='cd ~/BEAM && gradle shadowjar && \
	cp ~/BEAM/build/libs/beam.sim.Run.Beam.jar ~/beam_scenarios/beam.jar'
